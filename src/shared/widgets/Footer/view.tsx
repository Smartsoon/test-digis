'use client'

import { Box } from "@chakra-ui/react";

export const Footer: React.FC = ({}) => {
    return <footer>
        <Box height={100}
             backgroundColor={'lightgray'}
        />
    </footer>
}